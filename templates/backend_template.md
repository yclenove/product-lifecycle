# [BE-编号] 后端开发任务标题

> 💡 推荐先读 skill：`test-driven-development`、`systematic-debugging`、`chinese-commit-conventions`

| 字段 | 值 |
|------|-----|
| 版本 | v0.1（初稿） |
| 作者 | 后端工程师 |
| 日期 | YYYY-MM-DD |
| 状态 | 草稿 |
| 关联文档 | PRD-xxx、ARCH-xxx、DB-xxx、API-xxx |

---

## 1. 任务概述 [必填]

### 1.1 需求来源
> 引用 PRD 验收标准。

### 1.2 实现范围
> 包含 / 不包含。

### 1.3 技术决策
> 框架、ORM、缓存、队列。

## 2. 领域模型 [必填]

### 2.1 实体与值对象
| 实体 | 字段 | 不变式 |
|------|------|--------|

### 2.2 业务规则（纯领域逻辑）
> 列出关键业务规则，与基础设施无关。

## 3. API 设计 [必填]

| 方法 | 端点 | 说明 | 权限 | 限流 |
|------|------|------|------|------|
| POST | /api/orders | 创建订单 | user:write | 10/min |

### 3.1 请求/响应示例

```http
POST /api/orders
Authorization: Bearer xxx
Idempotency-Key: uuid

{
  "items": [...]
}
```

```json
{
  "data": { "order_id": "...", "status": "created" }
}
```

### 3.2 错误码

| 业务码 | HTTP | 含义 |
|--------|------|------|
| ORDER_INVENTORY_INSUFFICIENT | 400 | 库存不足 |

## 4. 数据访问 [必填]

### 4.1 SQL 变更
> 引用迁移文件。

### 4.2 关键查询
> 慢查询分析、索引使用。

### 4.3 缓存策略
| 数据 | 缓存键 | TTL | 失效策略 |
|------|--------|-----|----------|

## 5. 异步处理（如有） [条件]

| 任务 | 触发 | 队列 | 重试 |
|------|------|------|------|

## 6. 外部依赖 [条件]

| 服务 | 用途 | 超时 | 重试 | 熔断 |
|------|------|------|------|------|

## 7. 测试 [必填]

### 7.1 单元测试
> 领域逻辑覆盖率 ≥ 90%。

### 7.2 集成测试
> 用 Testcontainers 跑真实 DB / Redis。

### 7.3 契约测试（如有）
> OpenAPI validator / Pact。

### 7.4 负载测试
> k6 脚本 + 基线。

## 8. 性能 [必填]

| 指标 | 目标 | 实际 |
|------|------|------|
| P95 | < 200ms | |
| P99 | < 500ms | |
| 错误率 | < 0.1% | |
| QPS | ≥ X | |

## 9. 安全 [必填]

- [ ] 输入校验（schema）
- [ ] SQL 参数化
- [ ] 权限检查
- [ ] 限流
- [ ] 审计日志
- [ ] secret 不入代码

## 10. 可观测性 [必填]

- [ ] 关键路径有日志（结构化）
- [ ] 关键路径有 trace（OpenTelemetry）
- [ ] 关键指标有 metric
- [ ] 错误有告警规则

## 11. 文件清单 [必填]

> 本次变更涉及的所有文件列表。

## 12. 验证步骤 [必填]

```bash
# 启动
make run
# 健康检查
curl http://localhost:8080/health
# 业务验证
curl -X POST ...
```

## 13. 修订记录 [必填]

| 版本 | 日期 | 作者 | 变更说明 |
|------|------|------|----------|
| v0.1 | YYYY-MM-DD | 后端工程师 | 初稿 |
