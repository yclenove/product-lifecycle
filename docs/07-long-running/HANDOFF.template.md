# 跨 Session 移交单（HANDOFF）

> 在每个 session 结束前由当前 Agent 填写。下次 session 启动时由 resume.sh 读取。

## 本轮信息

| 字段 | 值 |
|------|-----|
| Session ID | <自定义或时间戳> |
| 开始时间 | YYYY-MM-DD HH:MM |
| 结束时间 | YYYY-MM-DD HH:MM |
| 主要 Agent | <agent-name> |
| 累计 tokens | <估算> |

## 本轮主要产出

- ✅ <成果 1>
- ✅ <成果 2>
- ✅ <成果 3>

## 本轮关键决策

| 决策 | 理由 | 影响 |
|------|------|------|
| 选 React 而不是 Vue | 团队熟悉度 + 生态 | 前端开发周期 -2 周 |
| 暂不引入 Redis | 当前 QPS < 100 | 上线后视情况引入 |

## 遗留 / 未决策项

按优先级排序，每项必须有「卡点」+「建议解法」。

### P0：<问题>

- **卡点**：<为啥这一轮没解决>
- **建议解法**：<下一轮怎么办>
- **责任人**：<谁来跟>

### P1：<问题>

- **卡点**：...
- **建议解法**：...

## 下次会话启动建议

```bash
# 1. 先恢复上下文
bash scripts/resume.sh

# 2. 然后跑下一个 Agent
> 调用 <下一个 agent> 继续

# 3. 重点关注以下产出（必读）
- docs/07-long-running/STATE.md
- docs/iterations/_legacy-by-role/product/PRD-YYYYMMDD-需求.md
- <其它关键文档>
```

## 给下一个 AI 的话（口语 ok）

> 上一轮我刚完成了 PRD 的初稿（v1），有 2 个点需要架构师重点评估：
> 1. 高并发下的库存扣减策略（架构 / dba 协作）
> 2. 第三方支付的容灾方案
>
> 客户对响应时间敏感，建议你在 arch 阶段就明确 P95 < 300ms 的约束。

## 风险预警

- ⚠️ <风险 1>：<描述>
- ⚠️ <风险 2>：<描述>

---

**填写规则**：

- 本文件每次 session 结束都要重写（覆盖上次）
- 想保留历史 handoff，请存到 `CHECKPOINTS/CKP-<date>-<phase>-done.md`
- 「给下一个 AI 的话」用自然语言即可，不需要结构化