---
name: pl
description: product-lifecycle 的短别名。中文触发：产品迭代、全流程开发。
when_to_use: "产品迭代, 全流程开发, 跑迭代, 开始迭代, 产品开发流程, market analysis, product iteration"
argument-hint: "[项目名] [一句话描述]"
allowed-tools: Agent WebSearch WebFetch Read Write Edit Glob Grep Bash TodoWrite
---

# product-lifecycle 短别名

**这是 `/product-lifecycle` 的快捷入口。**

请立即读取并执行 `~/.claude/skills/product-lifecycle/SKILL.md` 中的完整指令。

```
Read ~/.claude/skills/product-lifecycle/SKILL.md
```

然后按照里面的指引执行。不要跳过"启动前配置"步骤。
