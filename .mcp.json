{
  "_comment": "项目级 MCP server 配置（Claude Code 自动读取）。详见 docs/05-advanced/DIAGRAMMING.md",
  "mcpServers": {
    "drawio": {
      "command": "npx",
      "args": ["-y", "@next-ai-drawio/mcp-server@latest"]
    }
  }
}
