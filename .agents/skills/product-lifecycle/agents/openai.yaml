interface:
  display_name: "Product Lifecycle"
  short_description: "Coordinate product lifecycle roles"
  default_prompt: "Use $product-lifecycle to run a lean product lifecycle iteration for my project."

policy:
  allow_implicit_invocation: false
